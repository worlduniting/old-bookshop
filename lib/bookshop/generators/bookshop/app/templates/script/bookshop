# This command will automatically be run when you run "bookshop" with Bookshop gem installed from the root of your application.

APP_PATH = File.expand_path('../../',  __FILE__)

# Fragments variable for accessing fragments 
FRAGMENTS = "file://#{APP_PATH}/book/assets/fragments"

require 'pdfkit'

# configuration settings for PDFKit
PDFKit.configure do |config|
  # config.wkhtmltopdf = '/path/to/wkhtmltopdf'
  # config.default_options = {
  #   :page_size => 'Legal',
  #   :print_media_type => true
  # }
  # config.root_url = "http://localhost" # Use only if your external hostname is unavailable on the server.
  # config.toc_options = {
  #   :xsl_style_sheet => "#{FRAGMENTS}/toc.xsl"
  # }
  config.cover_options = {
    :file => "#{FRAGMENTS}/cover.html"
  }
end

require 'bookshop/commands'